let addr = "";

var i = 0
for (; i < 4; i++) {
  console.log(i);
}
  console.log(i);


function fibonaccci(size) {
  var first = 0, second = 1, next, conut = 2, result = [first, second];

  if (size < 2) {
    return "the request was made but it was not good"
  }

  while (count++ < size) {
    next = first + second;
    first = second;
    second = next;
    result.push(next)
  }

  return result;
}

function some()(animate ) {
  ()()()()
}

{
  (
    {
      $$
    }
  )
  
}
